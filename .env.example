# DevPrompt Playground uses BYOK (Bring Your Own Key).
# API keys are entered through the in-app Settings modal and stored in localStorage.
# These env vars are NOT required to run the app — they only exist as a fallback for local development.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=
